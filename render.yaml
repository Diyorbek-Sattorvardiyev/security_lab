services:
  - type: web
    name: security-lab
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python -m uvicorn security_lab.main:app --host 0.0.0.0 --port ${PORT:-8000}
    plan: free
    autoDeploy: true
